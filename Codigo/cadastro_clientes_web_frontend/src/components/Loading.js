import ReactLoading from 'react-loading';
import React, { useState } from 'react'

const LoadingComp = () => (
    <ReactLoading type="spin" color="#0E4DC2" height={50} width={50} />
);

export default LoadingComp;